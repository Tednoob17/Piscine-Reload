#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
        for i in range(0, len(matrix)):
                if (len(matrix)-1 == 0):
                        print("")
                else:
                        print(matrix[i])
