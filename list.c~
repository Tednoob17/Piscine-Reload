#incude <stdio.h>
#include "list.h"
#inlude <stdlib.h>
int main(void)
{
  t_list *list;
  list = NULL;

  list = add_link(list, "goku\n");
  list = add_link(list, "gohan\n");
  list = add_link(list, "goten\n");

  
  return (0);
}
