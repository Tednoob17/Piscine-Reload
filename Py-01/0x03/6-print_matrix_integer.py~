#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
        for element in matrix :
                if (!len(matrix)):
                        print("")
                else:
                        print(matrix[i])
