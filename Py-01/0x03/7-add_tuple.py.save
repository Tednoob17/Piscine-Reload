#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
	if len(tuple_a) > len(tuple_b):
		nb = len(tuple_a)
	
		if ((tuple_a[0])):
			tuple_a[i] = 0
		elif (type(tuple_b[i])) == None:
			tuple_b[i] = 0

	return(tuple_a[0] + tuple_b[0], tuple_a[1] + tuple_b[1])
